# Ansible Role - kemopq.hello_world.hello_world_role

Creates /tmp/testfile.txt on remote host with "Hello World" message.
The file is also signed with name and date (those data is inserted to file).
